name: 'Desafio 2 - Gatilhos Básicos'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  manutencao-diaria:
    name: Executar Tarefas de Manutenção
    runs-on: ubuntu-latest

    steps:
      - name: Verificar status do servidor
        run: |
          echo "🕒 Iniciando tarefas - $(date)"
          echo "🔍 Verificando status dos serviços..."

      - name: Limpar arquivos temporários
        run: echo "🧹 Limpando arquivos temporários..."

      - name: Backup do banco de dados
        run: |
          echo "💾 Iniciando backup..."
          echo "✅ Backup concluído!"

      - name: Notificação de conclusão
        run: |
          echo "🏁 Tarefas concluídas!"
          echo "📊 Relatório: $(date)"

      - name: Delay para visualização (somente manual)
        if: github.event_name == 'workflow_dispatch'
        run: sleep 15
